<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.chicko">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Chicko">
        <activity
                android:name=".AddServiceActivity"
                android:exported="false"
                android:label="@string/title_activity_add_service"
                android:theme="@style/Theme.Chicko.AppBarOverlay" />
        <activity
                android:name=".activities.CategoryProvidersActivity"
                android:exported="false" />
        <activity
                android:name=".activities.ServiceActivity"
                android:exported="false" />
        <activity
                android:name=".activities.MainActivity"
                android:exported="true"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>